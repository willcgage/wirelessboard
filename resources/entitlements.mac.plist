<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened runtime entitlements for the signed macOS app.

  Electron needs JIT and unsigned executable memory for V8. The bundled
  PyInstaller service (Contents/Resources/dist/wirelessboard-service) loads
  its own unsigned .so files and sets DYLD_* variables at startup, which the
  hardened runtime blocks unless library validation is disabled.

  These files live in resources/ rather than electron-builder's default
  buildResources directory because build/ is gitignored.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
